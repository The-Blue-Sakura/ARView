'''
We need to install python-3, python3-pip.
We then need to install the OpenCV, Numpy, picamera, and "picamera[array]" packages through PIP.
'''

# Import OpenCV
import cv2
# Print the version of OpenCV that we are using, to help us find the right documentation.
print(cv2.__version__)